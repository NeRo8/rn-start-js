let writeFileSync,mkdirSync,existsSync;_a26‍.x([["createFile",()=>createFile],["createFolder",()=>createFolder]]);_a26‍.w("fs",[["writeFileSync",["writeFileSync"],function(v){writeFileSync=v}],["mkdirSync",["mkdirSync"],function(v){mkdirSync=v}],["existsSync",["existsSync"],function(v){existsSync=v}]]);

//const colors = require("chalk");

function createFile(name = "index", type = "js", data) {
  writeFileSync(`${name}.${type}`, data);
}

function createFolder(path, files = []) {
  if (existsSync(path, existsSync(path))) {
  } else {
    //console.log(path, colors.green.bold("Created"));
    mkdirSync(path);
    files.forEach((file) => {
      const { name = "index", type = "js", data = "" } = file;
      writeFileSync(path + "/" + `${name}.${type}`, data);
      //console.log(path, colors.green.bold("Created"));
    });
  }
}


